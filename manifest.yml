---
applications:
  .:
    name: OmniDemo
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: !!null 
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      od_production:
        type: postgresql
      n:
        type: postgresql
